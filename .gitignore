# Perkakas uji (run-tests, seed-test-accounts, capture-api-samples, security-tests)
# dan hasil capture-nya SENGAJA ikut repo — isinya akun test + data seed, bukan
# data sekolah sungguhan. Kalau repo ini suatu saat dibuka ke publik, ganti dulu
# password akun test yang tercantum di api-sample-responses.md.

# Dokumen desain internal & brief kerja — bukan bagian dari repo
absensi-schema.md
backend-rbac-todo.md

# Output backup database (berisi data) — skrip backup-databases.ps1 boleh commit
db-backups/

# Artefak uji manual (mis. foto untuk uji upload) — jangan masuk repo
test_photo.jpg
test_*.jpg

# Config Claude Code lokal (berisi path absolut khas mesin) - jangan commit
.claude/
